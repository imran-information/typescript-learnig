// Object types

let user: { name: string, age: number } = {
    name: 'imran',
    age: 22
}

// literal type 
let status: 'success' = 'success';

// Optional Type 
let users: { name: string; age?: number } = {
    name: "Imran"
};